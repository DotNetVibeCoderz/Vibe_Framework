[package]
name = "rustnet-rndp"
description = "RNDP (RustNet Device Protocol) framing and command codes — no_std, shared by every firmware and the host transports"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
