<Window x:Class="RustNet.Designer.Editor.PromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RustNet" SizeToContent="Height" Width="340"
        WindowStyle="SingleBorderWindow" ResizeMode="NoResize"
        ShowInTaskbar="False" WindowStartupLocation="CenterOwner"
        Background="{StaticResource Slate}">
    <StackPanel Margin="14">
        <TextBlock x:Name="Label" Style="{StaticResource Readout}" Margin="0,0,0,6"/>
        <TextBox x:Name="Input" PreviewKeyDown="OnKeyDown"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Style="{StaticResource FlatButton}" Content="Cancel" Click="OnCancel"
                    Margin="0,0,6,0" Padding="14,5"/>
            <Button Style="{StaticResource PrimaryButton}" Content="OK" Click="OnOk" Padding="18,5"
                    IsDefault="True"/>
        </StackPanel>
    </StackPanel>
</Window>
